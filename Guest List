using System;
using System.Linq;
using System.Collections.Generic;
namespace GuestList
{
    class Program
    {
        static void Main(string[] args)
        {
            List<string> guest = new List<string>();
            int numberOfCommands = int.Parse(Console.ReadLine());
            string[] command;
            while(numberOfCommands > 0)
            {
                command = Console.ReadLine().Split(" ");
                if(command[2] == "going!")
                {
                    if (!guest.Contains(command[0])) guest.Add(command[0]);
                    else Console.WriteLine($"{command[0]} is already in the list!");
                }
                else
                {
                    if (guest.Contains(command[0])) guest.Remove(command[0]);
                    else Console.WriteLine($"{command[0]} is not in the list!");
                }
                numberOfCommands--;
            }
            foreach (string elm in guest)
                Console.WriteLine(elm);

        }
    }
}
